{
  "name": "mojang-blocklist",
  "version": "1.0.0",
  "description": "tracking and identifying blocklisted minecraft servers",
  "main": "index.js",
  "scripts": {
    "update-blocklist": "node update_blocklist.js",
    "update-merged": "node update_merged.js",
    "update-todo": "bash -c \"comm -23 <(cat data/todo.txt data/current.txt | sort -u) <(cut -d'=' -f1 data/identified.txt | sort -u) > data/todo2.txt && mv data/todo2.txt data/todo.txt\""
  },
  "author": "sudofox",
  "license": "MIT",
  "dependencies": {
    "sha1": "^1.1.1"
  }
}
